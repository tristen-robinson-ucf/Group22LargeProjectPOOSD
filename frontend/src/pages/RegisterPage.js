import React from 'react';

import PageTitle from '../components/PageTitle';
import Register from '../components/Register';

const RegisterPage = () =>
{

    return(
      <div>
        <PageTitle />
        <Register />
      </div>
    );
};
//this is a comment, add to it, add to it again
export default RegisterPage;
