import 'package:flutter/material.dart';

class PlannedTrips extends StatelessWidget{
  const PlannedTrips({ Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('Planned Trips here'),
      ),
    );
  }

}